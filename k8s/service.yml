# k8s/service.yml
apiVersion: v1
kind: Service
metadata:
  name: offscript-service
  namespace: offscript
  labels:
    app: offscript-api
spec:
  selector:
    app: offscript-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer